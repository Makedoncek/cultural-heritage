$kind: http-request
description: Partially update object. Author edit on approved object resets status to pending.
url: "{{baseUrl}}/api/objects/{{objectId}}/"
method: PATCH
body:
  mode: raw
  raw: |-
    {
      "title": "Оновлена назва"
    }
  options:
    raw:
      language: json
order: 4400
