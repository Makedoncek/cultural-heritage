$kind: http-request
description: Resend verification email for inactive accounts.
url: "{{baseUrl}}/api/auth/resend-verification/"
method: POST
auth:
  type: noauth
body:
  mode: raw
  raw: |-
    {
      "email": "{{email}}"
    }
  options:
    raw:
      language: json
order: 1700
