$kind: http-request
url: "{{baseUrl}}/api/auth/resend-verification/"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "{{email}}"
    }
auth:
  type: noauth
order: 7000
