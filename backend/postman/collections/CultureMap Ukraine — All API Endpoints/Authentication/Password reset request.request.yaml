$kind: http-request
url: "{{baseUrl}}/api/auth/password-reset/"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "{{email}}"
    }
auth:
  type: noauth
order: 5000
