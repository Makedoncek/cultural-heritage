$kind: collection
description: "Photo gallery endpoints for CultureMap Ukraine. Set variables:
  baseUrl, accessToken (JWT), objectId, photoId."
variables:
  baseUrl: http://localhost:8000
  accessToken: ""
  objectId: "1"
  photoId: ""
